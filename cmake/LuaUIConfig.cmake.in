@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(Threads)

if(WIN32)
    # Windows-specific dependencies
endif()

include("${CMAKE_CURRENT_LIST_DIR}/LuaUITargets.cmake")

check_required_components(LuaUI)
